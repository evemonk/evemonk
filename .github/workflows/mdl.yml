name: mdl

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  mdl:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: run mdl
        uses: actionshub/markdownlint@2.0.2
