# EveMonk Feature Flags (via flipper gem)

All external calls should be wrapper with feature flags.

This is current list with description:

| name      | description             |
|-----------|-------------------------|
| `:pghero` | Disable all pghero jobs |

